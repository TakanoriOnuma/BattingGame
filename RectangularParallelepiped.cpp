#include "RectangularParallelepiped.h"

RectangularParallelepiped::RectangularParallelepiped()
	: width(0.0), length(0.0), height(0.0)
{
}

RectangularParallelepiped::RectangularParallelepiped(double length, double width, double height)
	: length(length), width(width), height(height)
{
}